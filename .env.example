# Copia este archivo a `.env` en la raíz de Uco.parking (el archivo `.env` no se sube a Git).
# Misma contraseña que uses aquí debe ir en `application-secrets.properties` (spring.datasource.password).

MSSQL_SA_PASSWORD=
